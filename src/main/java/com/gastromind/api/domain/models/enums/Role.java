package com.gastromind.api.domain.models.enums;

/**
 * Enum con los roles disponibles para los usuarios.
 */
public enum Role {
    ROLE_ADMIN, ROLE_OWNER, ROLE_MEMBER, ROLE_PREMIUM_MEMBER, ROLE_TESTS
}
